open frontend folder with integrated terminal

install dependencies using folowing command -> 
npm i 

install sass globaly with following command ->
npm install -g sass

install folowing extentions on from vs code extentions ->
1. ESLint
2. Prettier ESLint

Run using following command ->
npm run dev
