import React from 'react';
import styled from 'styled-components';
const ButtonContainer = styled.button``;
const Button = () => {
  return <ButtonContainer>This is a Button</ButtonContainer>;
};

export default Button;
