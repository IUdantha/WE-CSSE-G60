import React from 'react';
import styled from 'styled-components';
const ButtonContainer = styled.button``;
const Button = () => {
  return <ButtonContainer>Add Vehicle</ButtonContainer>;
};

export default Button;
