import React from 'react';
import styled from 'styled-components';
const ButtonContainer = styled.button``;
const Button = () => {
  return <ButtonContainer>Add Salary</ButtonContainer>;
};

export default Button;
