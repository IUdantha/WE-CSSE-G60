import styled from 'styled-components';

const SectionContainer = styled.div`
  flex: 4;
  background-color: #ffffff;
`;
const Stock = () => {
  return <SectionContainer className="p-8 rounded-xl h-full">Stock</SectionContainer>;
};

export default Stock;
